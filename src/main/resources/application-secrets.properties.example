# Exemplo de application-secrets.properties
# Não commit: este arquivo contém placeholders para uso local.
# Copie para 'application-secrets.properties' e preencha com valores reais apenas na sua máquina ou em segredos do CI.

# Configurações do banco de dados
spring.datasource.username=YOUR_DB_USER
spring.datasource.password=YOUR_DB_PASSWORD
spring.datasource.url=jdbc:mysql://YOUR_DB_HOST:3306/YOUR_DB_NAME

# Liquibase
spring.liquibase.enabled=true

# Discord Bot Token (coloque o token real em application-secrets.properties local, não neste arquivo)
discord.token=YOUR_DISCORD_BOT_TOKEN

# Debug flags
app.debug.log-datasource=false

